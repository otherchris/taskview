<div class="container mx-auto px-4">
  <div class="flex justify-between items-center mb-2">
    <h1 class="text-2xl font-bold my-4">Daily Tasks</h1>
    <%= link_to "Create", new_daily_task_path, data: { turbo_frame: "new_daily_task" }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
  </div>

  <%= turbo_frame_tag "new_daily_task" %>

  <ul id="daily_tasks_list" class="list-disc list-inside bg-white p-4 rounded-lg shadow">
    <%= render @daily_tasks %>
  </ul>
</div> 